from calibre.web.feeds.news import BasicNewsRecipe

class JW_EN(BasicNewsRecipe):
    title = 'JW - Whats New EN'
    language = 'en'
    encoding = 'utf-8'

    # Diretrizes
    oldest_article = 9999
    max_articles_per_feed = 5
    no_stylesheets = True

    # Estrategia: Scraping + Limpeza Manual
    keep_only_tags = [
        dict(id='content') 
    ]

    # Feed unico
    feeds = [
        ('JW - Whats New EN', 'https://www.jw.org/en/whats-new/rss/WhatsNewWebArticles/feed.xml')
    ]
