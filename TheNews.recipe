from calibre.web.feeds.news import BasicNewsRecipe

class TheNews(BasicNewsRecipe):
    title = 'THE NEWS (Beehiiv)'
    language = 'pt-BR' # Ou 'en', dependendo do conteudo principal
    encoding = 'utf-8'

    # Diretrizes
    oldest_article = 9999
    max_articles_per_feed = 5
    no_stylesheets = True

    # Estrategia: Usar conteudo completo do RSS
    use_embedded_content = True 

    # Nao precisamos de keep_only_tags ou postprocess_html

    # Feed unico
    feeds = [
        ('THE NEWS (Beehiiv)', 'https://rss.beehiiv.com/feeds/j9teVW9Qmi.xml')
    ]
